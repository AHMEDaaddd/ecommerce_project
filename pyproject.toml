[tool.poetry]
name = "ecommerce_project"
version = "0.1.0"
description = "Учебный e-commerce проект на основе ООП"
authors = ["Your Name"]

[tool.poetry.dependencies]
python = "^3.10"

[tool.poetry.group.dev.dependencies]
pytest = "^8.0"
pytest-cov = "^4.1.0"
flake8 = "^7.0.0"
black = "^24.0.0"
isort = "^5.12.0"
mypy = "^1.8.0"
pydocstyle = "^6.3.0"

[tool.mypy]
strict = true
ignore_missing_imports = true
